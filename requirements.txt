# 核心爬虫引擎 (Playwright 及其 Python 绑定)
playwright>=1.40.0

# 阿里云 AI 总结与翻译 (使用 OpenAI 兼容接口)
openai>=1.3.0

# 修复时区问题 (GitHub Actions 运行环境必带，确保 Asia/Shanghai 可用)
tzdata

# 兜底总结方案 (可选)
duckduckgo_search==7.5.5

# 本地 Flask 仪表盘 (如不需要网页展示可删除)
flask>=2.3.0
apscheduler>=3.10.0
