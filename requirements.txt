# 核心采集：Playwright 浏览器自动化
playwright>=1.40.0

# AI 处理：阿里云兼容接口
openai>=1.3.0

# 环境加固：GitHub Actions 必须安装此包才能识别 Asia/Shanghai 时区
tzdata

# 可选：如果你的 Python 脚本中用到了正则的高级特性
# re 无需安装，内置

# 后端展示（如果部署在 Vercel 或本地调试需保留）
flask>=2.3.0
apscheduler>=3.10.0
